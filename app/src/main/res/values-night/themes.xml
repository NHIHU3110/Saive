<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Saive" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/colorNoirBlack</item>

        <!-- Material 3 Color Palette Overrides for Dark Mode -->
        <item name="colorPrimary">@color/colorMaroon</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/colorSand</item>
        <item name="colorTertiary">@color/colorLinen</item>
        <item name="android:colorBackground">@color/colorNoirBlack</item>
        <item name="colorSurface">@color/colorNoirBlack</item>
        <item name="colorOnSurface">@color/colorCotton</item>
    </style>
</resources>